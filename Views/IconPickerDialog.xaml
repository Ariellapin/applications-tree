<Window x:Class="WPFToolbarTree.Views.IconPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change icon"
        Width="560" Height="460"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,6"
                   Text="Look for icons in this file:"
                   Foreground="#404040"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FileBox" Padding="4,3"
                     VerticalContentAlignment="Center"
                     KeyDown="OnFileBoxKeyDown"/>
            <Button Grid.Column="1" Content="Load" Padding="10,3" Margin="6,0,0,0"
                    Click="OnLoadClick"/>
            <Button Grid.Column="2" Content="Browse..." Padding="10,3" Margin="6,0,0,0"
                    Click="OnBrowseClick"/>
        </Grid>

        <Border Grid.Row="2"
                BorderBrush="#C0C0C0" BorderThickness="1"
                Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ListBox x:Name="IconList"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionMode="Single"
                         MouseDoubleClick="OnIconDoubleClick">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="52" Height="56" Margin="2">
                                <Image Source="{Binding Image}"
                                       Width="32" Height="32"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       SnapsToDevicePixels="True"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>

        <TextBlock x:Name="StatusLabel" Grid.Row="3" Margin="0,6,0,0"
                   Foreground="#808080" FontSize="11"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Reset to default" Width="120" Margin="0,0,8,0"
                    Click="OnResetClick"/>
            <Button Content="Cancel" Width="80" Margin="0,0,8,0"
                    IsCancel="True"/>
            <Button x:Name="OkBtn" Content="OK" Width="80"
                    IsDefault="True" Click="OnOk"/>
        </StackPanel>
    </Grid>
</Window>
